name: Full ARMX 202 vs 203 extended validation

on:
  pull_request:
    paths:
      - 'ARMX/**'
      - 'models/**'
      - 'tests/**'
      - 'build_v5_5_kernel.cjs'
      - 'native/**'
      - '.github/workflows/v5-5-range.yml'
  workflow_dispatch:

concurrency:
  group: causal-202-203-extended-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Build compiled v5.5 kernel
        env:
          STONEFISH_CC: clang
        run: node build_v5_5_kernel.cjs
      - name: Verify contracts
        run: |
          node --check ARMX/ARMX-preview.js
          node --check ARMX/ARMX.js
          node tests/kernel_contract.js
          node tests/armx_contract.js
          node tests/armx_reply_policy.js
          node tests/native_contract.js

  extended:
    needs: contracts
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        scale: [202, 203]
        start: [100, 200, 300, 400, 500]
    name: scale ${{ matrix.scale }} start ${{ matrix.start }} — 100 games
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Set causal decision scale
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          p=Path("ARMX/ARMX.js")
          s=p.read_text()
          scale="${{ matrix.scale }}"
          s,n=re.subn(r"const ARMX_CAUSAL_DECISION_SCALE=\d+;",f"const ARMX_CAUSAL_DECISION_SCALE={scale};",s,count=1)
          if n != 1:
              raise SystemExit("causal decision scale constant not found exactly once")
          p.write_text(s)
          PY
      - name: Build compiled v5.5 kernel
        env:
          STONEFISH_CC: clang
        run: node build_v5_5_kernel.cjs
      - name: Run disjoint 100-game block
        env:
          GAMES: '100'
          START_INDEX: '${{ matrix.start }}'
          MATCHUP: 'artemisVsCurrent'
          RESULT_JSON: result-${{ matrix.scale }}-${{ matrix.start }}.json
        run: node tests/v5_5_range.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: causal-${{ matrix.scale }}-start-${{ matrix.start }}
          path: result-${{ matrix.scale }}-${{ matrix.start }}.json
          if-no-files-found: ignore
