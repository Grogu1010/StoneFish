name: v4-tuning-convert-lead
on:
  push:
    branches: ['v4-tuning-convert-lead']
  workflow_dispatch:

jobs:
  tune:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node tools/v4-convert-lead.js
