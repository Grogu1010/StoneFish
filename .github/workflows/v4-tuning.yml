name: v4-tuning-fiveply-trade
on:
  push:
    branches: ['v4-tuning-fiveply-trade']
  workflow_dispatch:

jobs:
  tune:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node tools/v4-fiveply-trade.js
