name: v45-prod-verify
on:
  push:
    branches: [v45-prod-verify]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/v45-prod-verify.js
